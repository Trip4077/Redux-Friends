const initialState = {
  fetchFriends: false,
  friendsFetched: false,
  friends: [],
  error: ''
}

export const getFriendReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state
  }
}
